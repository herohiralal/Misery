#include "Dx12Private.h"

#if GPU_DX12

void GPU_Dx12CreateBuffer(GPU_Buffer* outBaseBuffer, GPU_Instance* baseRenderer, GPU_BufferCfg cfg) { MSR_ASSERT(false); }
void GPU_Dx12DestroyBuffer(GPU_Buffer* baseBuffer) { MSR_ASSERT(false); }

#endif
